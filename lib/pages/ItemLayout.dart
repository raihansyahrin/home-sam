import 'package:flutter/material.dart';

class ItemLayout extends StatelessWidget {
  const ItemLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 200,
      height: 200,
      color: Colors.red,
    );
  }
}
